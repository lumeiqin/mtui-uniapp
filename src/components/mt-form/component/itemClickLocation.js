import itemClickLocation from "../item-click-location/item-click-location";

itemClickLocation.install = function (Vue) {
    Vue.component(itemClickLocation.name, itemClickLocation);
}

export default itemClickLocation;
