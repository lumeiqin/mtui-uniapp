import itemMultiSelector from "../item-multiSelector/item-multiSelector";

itemMultiSelector.install = function (Vue) {
    Vue.component(itemMultiSelector.name, itemMultiSelector);
}

export default itemMultiSelector;
