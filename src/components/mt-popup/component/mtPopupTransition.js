import mtPopupTransition from "../mt-popup-transition";

mtPopupTransition.install = function(Vue) {
    Vue.component(mtPopupTransition.name, mtPopupTransition);
};

export default mtPopupTransition;
